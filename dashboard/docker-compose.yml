version: "3.9"

services:
  app:
    build: .
    container_name: unusual-suspects-app
    ports:
      - "8501:8501"
    restart: unless-stopped

    environment:
      DB_HOST: ethereum-db.cnwm886wwadv.us-east-1.rds.amazonaws.com
      DB_PORT: 3306
      DB_NAME: ethereum_db
      DB_USER: etl_user
      DB_PASSWORD: Unusual_suspects!2025

